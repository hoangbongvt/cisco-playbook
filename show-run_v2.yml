---
- name: Show running configuration
  hosts: all
  gather_facts: no
  tasks:
    - name: Run show running-config
      cisco.ios.ios_command:
        commands:
          - show running-config
      register: show_run_output

    - name: Display the running configuration output
      debug:
        var: show_run_output
